name: tiny_compiler
description: Tiny Scanner
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dart_vlc: ^0.1.9
  filepicker_windows: ^2.0.0
  provider: ^6.0.2

dev_dependencies:
  flutter_lints: ^1.0.4

flutter:
  uses-material-design: true

  assets:
    - assets/images/dots_vert.png
    - assets/audio/meow01.wav
