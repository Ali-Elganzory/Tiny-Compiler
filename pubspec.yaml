name: tiny_compiler
description: Compiler Frontend (Tiny)
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  # dart_vlc: ^0.1.9
  provider: ^6.0.2
  file_picker: ^4.5.1
  collection: ^1.15.0

dev_dependencies:
  flutter_lints: ^1.0.4

flutter:
  uses-material-design: true

  assets:
    - assets/images/dots_vert.png
    # - assets/audio/meow01.wav
